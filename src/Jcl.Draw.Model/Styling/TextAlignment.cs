namespace Jcl.Draw.Model.Styling;

/// <summary>Horizontal alignment of text within a shape.</summary>
public enum TextAlignment
{
    Left = 0,
    Center = 1,
    Right = 2,
}

/// <summary>Vertical alignment of text within a shape.</summary>
public enum TextVerticalAlignment
{
    Top = 0,
    Center = 1,
    Bottom = 2,
}
