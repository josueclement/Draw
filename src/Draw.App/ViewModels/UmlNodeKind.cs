namespace Draw.App.ViewModels;

/// <summary>Which UML structural node a toolbox tool creates (toolbox/creation dispatch only).</summary>
public enum UmlNodeKind
{
    Package,
    Component,
    Deployment,
}
